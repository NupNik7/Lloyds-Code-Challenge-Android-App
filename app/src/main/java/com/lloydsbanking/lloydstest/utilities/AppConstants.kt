package com.lloydsbanking.lloydstest.utilities

/**
 * App wide constants
 */
object AppConstants {
    const val BASE_URL = "https://api.coincap.io/v2/"
    const val DEFAULT_NUMBER_OF_RETRIES: Long = 3
    const val DEFAULT_DELAY_MILLIS: Long = 500
    const val DEFAULT_DELAY_FACTOR: Long = 2
}